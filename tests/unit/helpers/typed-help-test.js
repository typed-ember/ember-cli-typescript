import { typedHelp } from 'dummy/helpers/typed-help';
import { module, test } from 'qunit';

module('Unit | Helper | typed help');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = typedHelp([42]);
  assert.ok(result);
});
